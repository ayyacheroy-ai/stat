# Optional. When unset (the default), the app reads the bundled CSV at
# src/data/csv/sample-match.csv and keeps CSV uploads in memory — no
# configuration needed to run locally, exactly as before.
#
# Set this to point the frontend at the real backend in /backend instead
# (e.g. http://localhost:4000 when running it locally). See
# src/lib/data-source.ts and backend/README.md.
# PITCHLINE_API_URL=http://localhost:4000
