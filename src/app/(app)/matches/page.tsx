import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function MatchesPage() {
  return (
    <PlaceholderScreen
      title="Matches"
      description="The matches list and match overview are coming in a later build stage. For now, check Home for today's tracked players."
    />
  );
}
