import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function PlayersPage() {
  return (
    <PlaceholderScreen
      title="Players"
      description="A full players list and deep player profiles are coming in a later build stage. For now, check Home for today's tracked players."
    />
  );
}
